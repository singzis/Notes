虚拟DOM是一种用于在浏览器端渲染页面的技术，它用javascript对象描述真实DOM树的数据结构，在页面需要更新时，会生成新的虚拟DOM树，然后与旧的DOM树比较，找出实际需要更新的部分，然后将更新应用到真实DOM树中，这么做减少了对真实DOM树的操作，增加了渲染性能

虚拟DOM的工作过程大概可以概括：
1. 应用程序状态发生变化，需要更新页面内容
2. 应用程序根据新的状态生成新的虚拟DOM树
3. 比较新旧虚拟DOM树，找出实际变化的部分
4. 将变化应用到真实DOM树中
5. 页面内容发生变化，用户看到更新后的页面